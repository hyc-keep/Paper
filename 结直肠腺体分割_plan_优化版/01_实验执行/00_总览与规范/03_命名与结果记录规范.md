# 命名与结果记录规范

## 1. 文件角色与执行边界

- 本文件负责统一 `run_name`、结果表字段、版本字段、实验资产命名和记录口径。
- 本文件不替代单阶段训练协议、设计依据、阶段验收或回退裁决。

## 2. 本轮直接依赖的前置文件

- 直接依赖 `结直肠腺体分割_plan_优化版/01_实验执行/00_总览与规范/00_执行导航.md`、`结直肠腺体分割_plan_优化版/01_实验执行/00_总览与规范/01_工程目录框架.md`、`结直肠腺体分割_plan_优化版/01_实验执行/00_总览与规范/02_参数冻结总表.md`。
- 同时约束 `crc_gland_segmentation_project/experiments/`、`crc_gland_segmentation_project/reports/`、`crc_gland_segmentation_project/scripts/` 的正式输出命名。

## 3. 本阶段唯一允许处理的变量

- 只允许统一命名规则、版本字段、结果记录字段和正式资产文件名，不允许在本文件里重开模型设计、训练策略或评估定义。

## 4. 阶段门控表达式

- 后续阶段必须能依据本文件稳定生成可回链的 `run_name`、`run_meta.yaml`、结果表和图像资产命名，本文件才算有效。

这份文件负责把后续代码工程里最容易失控的内容一次写死：

- `run_name` 怎么命名
- 协议版本怎么记录
- 结果目录怎么落盘
- checkpoint / log / metrics / visual 文件名怎么统一
- 哪些字段必须写进配置和结果表

它不负责给训练参数找论文依据，那部分应回到：

- `02_路线与投稿`
- `03_文献证据`
- `结直肠腺体分割_plan_优化版/01_实验执行/00_总览与规范/06_实验执行证据化写作模板.md`

这份文件的定位非常明确：

> 它属于 `工程冻结规则`，目的是保证后续代码、实验、汇总、写作用的是同一套命名与记录协议。

---

## 1. 使用原则

后面任何一个想进入正式结果层的 run，都必须同时满足下面三条：

1. 能从名字看出阶段、模型、数据集、协议版本、seed
2. 能在固定目录结构下找到配置、日志、权重、指标和可视化
3. 能在结果表里追溯到唯一的协议版本和唯一的 checkpoint

如果一个结果只剩一个截图、一个最好数字或一个随手起的目录名，它默认不具备进入正式汇总层的资格。

---

## 2. `run_name` 统一规则

### 2.1 标准格式

后面正式 run 统一使用下面格式：

```text
[stage_code]_[model_code]_[dataset_code]_[proto_version]_[seed_tag]
```

例如：

- `A1_UNet_GlaS_v1_seed3407`
- `A2_UNet_GlaS_v1_seed1234`
- `B1_R34UNet_GlaS_v1_seed2025`
- `H1_UNetPP_GlaS_v1_seed3407`
- `I1_Final_CRAG_v1_seed3407`

### 2.2 各字段含义

#### `stage_code`

只允许用固定阶段码：

- `A1`：`UNet` 首轮跑通
- `A2`：`UNet` 三次重复稳定
- `B1`：`ResNet34-U-Net` baseline
- `C1`：`LKMA`
- `D1`：`Boundary`
- `E1`：`Distance`
- `H1`：第一批外部对比
- `I1`：`CRAG`
- `J1`：结果汇总或补表重算

如果后面某阶段有第二版协议，升级版本号，不另外乱造阶段码。

#### `model_code`

统一采用短名，不允许同一模型多套写法混用。

固定写法：

- `UNet`
- `R34UNet`
- `R34UNet_LKMA`
- `R34UNet_LKMA_Boundary`
- `R34UNet_LKMA_Boundary_Dist`
- `UNetPP`
- `DeepLabV3P`
- `AttUNet`
- `nnUNet`
- `TransUNet`
- `SwinUNet`
- `Final`

禁止写法：

- `resnet34_unet`
- `res34unet`
- `deeplabv3+`
- `attention_u_net`

因为后面结果汇总会出现字符串分裂问题。

#### `dataset_code`

只允许：

- `GlaS`
- `CRAG`

#### `proto_version`

只记录当前 run 对应的正式协议版本，例如：

- `v1`
- `v2`

规则：

- 只要训练、评估、增强、后处理或模型结构中的正式协议发生变化，就必须升级版本
- 如果只是换 seed，不升级版本
- 如果只是同一协议下重复运行，不升级版本

#### `seed_tag`

正式 seed 固定写法：

- `seed3407`
- `seed1234`
- `seed2025`

---

## 3. 协议版本记录规则

### 3.1 后面至少要同时记录 8 类版本

每个正式 run 的配置或结果记录里，至少应明确写出：

- `config_version`
- `data_proto_version`
- `train_proto_version`
- `aug_version`
- `eval_proto_version`
- `model_version`
- `loss_version`
- `postprocess_version`

固定示例：

```yaml
config_version: v1
data_proto_version: data_proto_v1
train_proto_version: train_proto_v1
aug_version: main_aug_v1
eval_proto_version: eval_proto_v1
model_version: r34unet_lkma_boundary_v1
loss_version: seg_loss_v1
postprocess_version: none_in_v1
```

### 3.2 什么情况下必须升级版本

只要出现下面任意一种变化，就必须升级相关版本号：

- 数据划分变了
- 标签生成逻辑变了
- 增强内容或概率变了
- checkpoint 选择规则变了
- threshold 规则变了
- 后处理从关闭改为开启
- 模型结构真正变了

不允许出现下面情况：

- 改了协议，但还沿用旧 `v1`
- 结果已经混到主表里，才发现不同 run 根本不是同一协议

### 3.3 `run_name` 里的 `proto_version` 怎么解释

`run_name` 里的 `[proto_version]` 统一只写当前正式 `config_version` 的主版本号，例如 `v1`、`v2`。

详细拆分版本必须进入：

- config.yaml
- run_meta.yaml

这样规定的原因是：

- `run_name` 需要短、稳、可读，不适合把 `data / train / aug / eval / model / loss / postprocess` 全部拼进目录名
- 但正式配置和结果记录又必须保留完整版本链，避免“同一个 `v1` 实际上不是同一协议”

固定理解方式：

- `run_name = A2_R34UNet_GlaS_v1_seed3407` 里的 `v1` 只表示这次正式 run 隶属 `config_version = v1`
- 该 run 的详细协议差异再由 `data_proto_version / train_proto_version / aug_version / eval_proto_version / model_version / loss_version / postprocess_version` 追溯

---

## 4. 实验目录统一规则

### 4.1 正式目录结构

后面正式 run 必须统一落到：

```text
experiments/
└─ [run_name]/
   ├─ config.yaml
   ├─ seed_record.txt
   ├─ run_meta.yaml
   ├─ train_log.csv
   ├─ val_metrics.csv
   ├─ testA_metrics.csv        # 仅 GlaS
   ├─ testB_metrics.csv        # 仅 GlaS
   ├─ test_metrics.csv         # 仅 CRAG
   ├─ metric_crosscheck_note.md
   ├─ checkpoints/
   │  ├─ best.ckpt
   │  └─ last.ckpt
   ├─ predictions/
   │  ├─ val/
   │  ├─ testA/
   │  ├─ testB/
   │  └─ test/
   ├─ visuals/
   │  ├─ val/
   │  ├─ testA/
   │  ├─ testB/
   │  └─ test/
   ├─ curves/
   ├─ summaries/
   │  ├─ run_summary.md
   │  └─ error_cases.md
   └─ artifacts/
```

### 4.2 固定说明

- `GlaS` 必须分开保存 testA_metrics.csv 和 testB_metrics.csv
- `CRAG` 只有一个正式测试集，因此固定为 test_metrics.csv
- metric_crosscheck_note.md 固定放在 run 根目录，避免评估口径留痕和训练总结分家
- `best.ckpt` 和 `last.ckpt` 统一使用 `.ckpt`
- summary.md 统一收敛为 summaries/run_summary.md
- error_cases.md 固定放在 `summaries/`

### 4.3 为什么这样统一

因为当前文档里已经出现过这些冲突：

- `best.pth` vs `best.ckpt`
- training_log.csv vs train_log.csv
- val_log.csv vs val_metrics.csv
- test_metrics.csv vs `testA_metrics.csv / testB_metrics.csv`

后面一律按本文件统一，不再多写法并存。

---

## 5. 文件名统一规则

### 5.1 checkpoint

固定为：

- `best.ckpt`
- `last.ckpt`

不再使用：

- `best.pth`
- `last.pth`

### 5.2 日志

固定为：

- train_log.csv
- val_metrics.csv

说明：

- 训练过程的逐 epoch 损失和学习率进 train_log.csv
- 用于选 best model 的验证指标进 val_metrics.csv

不再使用：

- training_log.csv
- val_log.csv

### 5.3 测试指标

固定为：

- testA_metrics.csv
- testB_metrics.csv
- test_metrics.csv

### 5.4 结果摘要

固定为：

- summaries/run_summary.md
- summaries/error_cases.md

### 5.5 图像文件

每张导出图统一至少包含下面字段：

```text
[sample_id]_[split]_[view_type].png
```

例如：

- `testA_001_overlay.png`
- `testB_014_pred.png`
- `test_023_boundary.png`

固定 `view_type`：

- `image`
- `gt`
- `pred`
- `overlay`
- `boundary`
- `error`

---

## 6. 配置文件最少字段

后面每个正式 run 的 config.yaml 至少要包含下面字段：

```yaml
run_name:
stage:
dataset:
model_name:
model_version:
config_version:
data_proto_version:
train_proto_version:
aug_version:
aug_profile_name:
eval_aug_enable:
eval_proto_version:
eval_cast_policy:
loss_version:
seed:
input_size:
loss:
optimizer:
lr:
weight_decay:
scheduler:
batch_size:
amp:
best_selector:
threshold_source:
threshold_value:
boundary_metric_width:
boundary_metric_impl:
connected_components_impl:
connected_components_connectivity:
postprocess_version:
```

如果某个正式 run 缺少这些字段中的关键项，后面不允许进结果表。

### 6.1 `best_selector` 允许值

当前正式主线只允许：

- `val_objdice_max`

这条规则必须和 `结直肠腺体分割_plan_优化版/01_实验执行/00_总览与规范/04_评估口径与官方脚本对齐.md` 保持一致。

暂不允许：

- `val_loss_min`
- `val_dice_max`
- `val_f1_max`

除非后面显式升级评估协议版本，并回改总览层规范。

### 6.2 `threshold_source` 允许值

当前正式主线只允许：

- `val17`
- `val20`

对应关系固定为：

- `GlaS` -> `val17`
- `CRAG` -> `val20`

暂不允许：

- `testA`
- `testB`
- `test`
- `manual`

因为阈值只能来自验证集，不能由测试集回调，也不能靠手工试出来。

---

## 7. run_meta.yaml 最少字段

为了让结果汇总脚本稳定复用，每个正式 run 都必须额外导出一个轻量元信息文件：

```yaml
run_name:
status:
start_time:
end_time:
git_or_snapshot_id:
config_version:
data_proto_version:
train_proto_version:
aug_version:
aug_profile_name:
eval_aug_enable:
eval_proto_version:
eval_cast_policy:
device:
num_epochs_trained:
best_epoch:
best_selector:
best_metric_value:
threshold_source:
threshold_value:
boundary_metric_width:
boundary_metric_impl:
connected_components_impl:
connected_components_connectivity:
metric_crosscheck_done:
metric_crosscheck_result:
metric_crosscheck_note_path:
seed_reporting_mode:
main_seed_set:
additional_seed_set:
combined_seed_count:
checkpoint_path:
notes:
```

说明：

- `status` 只允许：`running / done / failed / aborted`
- `best_selector` 必须写清具体规则，例如 `val_objdice_max`
- `threshold_source` 只允许来自验证集
- `boundary_metric_width` 主版本固定记录 `3`
- `metric_crosscheck_done` 不允许只写口头“已对齐”；必须能回指 `metric_crosscheck_note_path`
- `seed_reporting_mode` 默认记录冻结主协议统计口径，若进入补充 reporting，必须同步记录 `additional_seed_set / combined_seed_count`
- `checkpoint_path` 必须指向 `best.ckpt`

### 7.1 结果作废与重跑规则

如果后面发现下面任意一种情况，该 run 默认不能继续留在正式主表：

- `best_selector` 与总览层冻结规则不一致
- `threshold` 来源不是验证集
- `config_version / eval_proto_version` 没有随协议变化升级
- 结果目录文件名不符合当前命名规范

出现这些问题时，必须执行二选一：

1. 作为旧协议 run 单独归档，不再混入当前主表
2. 按当前协议重跑并生成新的 `run_name`

---

## 8. 结果表字段统一规则

### 8.1 单次 run 记录字段

后面任何汇总表，只要记录单次 run，至少要有：

| 字段 | 说明 |
|------|------|
| `run_name` | 唯一运行名 |
| `stage` | 阶段名 |
| `dataset` | `GlaS` 或 `CRAG` |
| `model_name` | 统一模型名 |
| `config_version` | 当前正式协议版本 |
| `train_proto_version` | 当前训练协议版本 |
| `aug_profile_name` | 正式增强 profile 名 |
| `eval_aug_enable` | 评估侧是否允许增强，当前主线固定为 `false` |
| `eval_proto_version` | 当前评估协议版本 |
| `seed` | 当前 seed |
| `split_role` | `val / testA / testB / test` |
| `best_selector` | best model 选择规则 |
| `threshold_value` | 最终阈值 |
| `boundary_metric_width` | `Boundary F1` 正式评估宽度，当前主线固定为 `3 px` |
| `metric_crosscheck_result` | `pass / minor_gap_documented / fail` |
| `checkpoint_path` | `best.ckpt` 路径 |
| `result_tag` | `reproduced / † / *` |
| `aggregation` | `single_seed / mean+-std` |
| `metrics` | 对应指标 |
| `note` | 异常与补充说明 |

### 8.2 多 seed 汇总字段

多 seed 表至少要有：

| 字段 | 说明 |
|------|------|
| `model_name` | 模型名 |
| `config_version` | 统一协议版本 |
| `train_proto_version` | 统一训练协议版本 |
| `eval_proto_version` | 统一评估协议版本 |
| `dataset` | 数据集 |
| `split_role` | 测试角色 |
| `seeds` | 参与统计的 seeds |
| `seed_reporting_mode` | `fixed_3_seed_main / fixed_3_seed_main + additional_reporting` |
| `main_seed_set` | 主协议冻结 seed 集 |
| `additional_seed_set` | 若有补充 reporting，显式记录额外 seed 集 |
| `metric_name` | 指标名 |
| `mean` | 均值 |
| `std` | 标准差 |
| `n_runs` | 重复次数 |
| `aggregation` | 固定写 `mean+-std` |
| `result_tag` | 通常写 `reproduced` |

### 8.3 引用值、复现值与统计方式标记

后面结果表里，不允许只写一个数字而不说明它到底是什么身份。

固定标记规则：

- `reproduced`：你自己按当前统一协议跑出来的结果
- `†`：直接引用原文报告值
- `*`：原文自带后处理、特殊测试流程或其它需要额外提醒的设置

固定统计规则：

- `A1`、第一轮外部筛查等排雷用途结果，可以记为 `single_seed`
- 进入正式主线比较层的 `A2 / B1 / C1 / D1 / E1 / I1 / J1` 结果，默认应收敛为 `mean+-std`
- 如果同一张表里同时出现 `single_seed`、`mean+-std` 和 `† quoted` 值，必须额外保留 `aggregation` 与 `result_tag` 字段，或在表注中等价说明

这样做的目的不是让表更复杂，而是防止下面三种混写：

- 把单次试跑值冒充稳定结论
- 把原文引用值冒充当前统一协议复现值
- 把带特殊后处理的数字和无后处理数字写成同一口径

---

## 9. 代码侧必须统一的命名常量

后面代码里必须统一设一组常量，不要把文件名散落在各脚本里。

固定统一常量：

```python
BEST_CKPT_NAME = "best.ckpt"
LAST_CKPT_NAME = "last.ckpt"
TRAIN_LOG_NAME = "train_log.csv"
VAL_METRICS_NAME = "val_metrics.csv"
TESTA_METRICS_NAME = "testA_metrics.csv"
TESTB_METRICS_NAME = "testB_metrics.csv"
TEST_METRICS_NAME = "test_metrics.csv"
RUN_SUMMARY_NAME = "run_summary.md"
ERROR_CASES_NAME = "error_cases.md"
```

原因：

- 避免某个脚本写 `.pth`，另一个脚本写 `.ckpt`
- 避免某个阶段还在用旧命名
- 方便后续统一汇总

---

## 10. 当前明确禁止的做法

- 同一模型出现两种以上 `run_name` 写法
- 改了协议版本但不改 `config_version`
- checkpoint 用 `best.pth`，结果表却写 `best.ckpt`
- `GlaS` 只保留一个总测试值，不分 `TestA / TestB`
- `CRAG` 结果沿用 `testA / testB` 命名
- 同一张表里把 `single_seed`、`mean+-std` 和 `†` 引用值混成一种无标记数字
- 汇总脚本靠手工改字符串适配不同文件名
- 先看测试结果，再回头改 `best checkpoint`

---

## 11. 验收清单

后面每完成一个阶段，至少检查下面这些：

1. 新 run 是否严格符合 `run_name` 模板
2. config.yaml 是否含完整版本字段
3. `checkpoints/` 是否统一生成 `best.ckpt` 和 `last.ckpt`
4. `GlaS` 是否分开导出 testA_metrics.csv 和 testB_metrics.csv
5. `CRAG` 是否统一导出 test_metrics.csv
6. 汇总模板是否能不改代码直接读取这些文件

如果这 6 条里有任意一条失败，就说明命名规范还没有真正落地。

---

## 12. 冲突裁决记录

- 冲突对象：`run_name` 中的简写版本号、`config.yaml / run_meta.yaml` 中的详细协议版本链，以及结果表中的统计身份标记
- 冲突来源：当前文件旧版虽然冻结了 `run_name`、目录树和基础文件名，但还没有把 `config_version / data_proto_version / eval_proto_version` 之外的 `train_proto_version / loss_version / postprocess_version` 纳入统一记录，也没有显式规定 `reproduced / † / *` 与 `single_seed / mean+-std` 的记录规则；这会和 `结直肠腺体分割_plan_优化版/01_实验执行/00_总览与规范/02_参数冻结总表.md`、`结直肠腺体分割_plan_优化版/01_实验执行/00_总览与规范/04_评估口径与官方脚本对齐.md`、`结直肠腺体分割_plan_优化版/01_实验执行/02_UNet流程验证/00_阶段总协议.md` 中已经出现的协议版本字段和统计口径发生分裂
- 裁决结论：`run_name` 中的 `[proto_version]` 只保留 `config_version` 的主版本号；详细协议版本统一写入 config.yaml 与 run_meta.yaml；结果表必须显式记录 `result_tag` 与 `aggregation`，区分 `reproduced / † / *` 以及 `single_seed / mean+-std`
- 裁决理由：目录名需要简短稳定，不能把所有协议字段都压进 `run_name`；但正式实验又必须保证训练协议、评估协议、损失版本和后处理状态都可追溯。如果不把简写层和详细记录层分开，后续会再次出现“同名目录、不同协议”或“同表数字、不同身份”的问题
- 受影响文件：`结直肠腺体分割_plan_优化版/01_实验执行/00_总览与规范/03_命名与结果记录规范.md`，`结直肠腺体分割_plan_优化版/01_实验执行/00_总览与规范/02_参数冻结总表.md`，`结直肠腺体分割_plan_优化版/01_实验执行/00_总览与规范/04_评估口径与官方脚本对齐.md`，`结直肠腺体分割_plan_优化版/01_实验执行/02_UNet流程验证/00_阶段总协议.md`，`10_结果汇总/*`
- 是否需要回流修订：需要；后续进入 `02_UNet流程验证`、`08_外部对比` 和 `10_结果汇总` 时，必须把仍未记录 `train_proto_version / loss_version / result_tag / aggregation` 的口径同步补齐
- 代码实现影响：影响 config.yaml、run_meta.yaml、汇总表 schema、阶段汇总脚本字段，以及结果表对 `single_seed / mean+-std / quoted` 数字的自动识别逻辑

---

## 13. 文件质量自检

- [x] 已遍历 `02_路线与投稿` 的相关文件
- [x] 已遍历 `03_文献证据` 的相关正式深提取稿
- [x] 已检查 `01_实验执行` 全局相关文件，包括前面和后面的内容
- [x] 对后续文件中的相关内容做了甄别，而不是机械继承
- [x] 发现冲突时，已经补写标准化 `冲突裁决记录`
- [x] 已区分 `官方协议固定项 / 路线层已锁定 / 论文直接支持 / 论文支持的候选范围 / 工程冻结规则 / 待确认项`
- [x] 已写清当前文件最核心规则的来源
- [x] 已写清为什么这样设计，而不是只给结论
- [x] 已写清公式、定义或原理解释（如果该文件涉及这些内容）
- [x] 公式、定义或原理已经达到“可直接翻译代码 + 可直接写入论文”的最低深度
- [x] 关键符号、术语、版本名和代码字段与前后文件保持一致
- [x] 已写清参数、结构、训练、评估或数据规则的作用
- [x] 已写清为什么不采用相邻方案（如果该文件涉及方案选择）
- [x] 已写清代码落点和运行记录字段
- [x] 代码落地对象已经细化到入口函数/类、I/O、配置字段和运行产物
- [x] 已写清验收方式与回退条件
- [x] 当前修改影响到的其它文件，已经列出并同步回改或显式标记待回改
- [x] 所有 `待确认项` 都已写清关闭截止阶段、阻塞动作和回流修订要求
- [x] `结直肠腺体分割_plan_优化版/01_实验执行/00_总览与规范/06_实验执行证据化写作模板.md` `§7` 的检查项与一票否决项已同步核对
- [x] 当前文件已经达到“可直接指导代码与论文写作”的最小强度

---

## 14. 一句话版本

> 这份文件把后续代码工程中的 `run_name`、协议版本链、目录结构、checkpoint、日志、指标文件和结果表字段全部冻结成唯一规则；从现在开始，任何正式实验结果都必须能按这套命名与记录协议被唯一追溯、唯一汇总、唯一复查，并且能明确区分 `single_seed / mean+-std / reproduced / quoted` 的真实身份。
